export function getBaseLocation() {
    // if(window != undefined && window.location != undefined){
    // let paths: string[] = location.pathname.split('/').splice(1, 1);
    // let basePath: string = (paths && paths[0]) || 'universal'; // Default: my-account
    // return '/' + basePath;
    // }else{
        return '/shop' ;
    //}
}